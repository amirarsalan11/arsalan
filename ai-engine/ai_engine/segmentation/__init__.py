"""
Floor Segmentation stage module.

Responsibility (per architecture lock):
Floor segmentation using SegFormer-B2. Model loading is separated from inference; never load the model per request.

Milestone 1 scope: this module is an empty placeholder package.
No implementation, model loading, or image-processing logic is included
here. Each pipeline stage will be implemented independently in its own
milestone (see MVP Development Order), keeping input/output boundaries
clean and testable rather than a single monolithic process_image()
function.
"""
